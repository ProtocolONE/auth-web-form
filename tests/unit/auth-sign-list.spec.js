import factory from './utils/components-factory'
import isVisible from './utils/is-visible'

// import AuthSignList from '@/components/AuthSignList'

describe('AuthSignList', function () {
  // let wrapper = factory(AuthSignList)
  // let toggle = wrapper.find('.sign-toggle')
  // let accordion = wrapper.find('.sign-accordion')

  // it('Check accordion toggle', () => {
  //   expect(isVisible(accordion)).toBeFalsy()
  //   toggle.trigger('click.prevent')
  //   expect(isVisible(accordion)).toBeTruthy()
  //   toggle.trigger('click.prevent')
  //   expect(isVisible(accordion)).toBeFalsy()
  // })

  it('Test', () => {
    expect(1).toEqual(1)
  })
})
