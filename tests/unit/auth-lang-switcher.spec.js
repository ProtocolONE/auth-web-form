// import factory from './utils/components-factory'
// import AuthLangSwitcher from '@/components/AuthLangSwitcher'

describe('AuthSignList', function () {
  // let wrapper = factory(AuthLangSwitcher)
  //
  // it('Check than switchers length equal available locales length', () => {
  //   expect(wrapper.findAll('.base-link').length).toBe(wrapper.vm.$i18n.availableLocales.length)
  // })

  it('Test', () => {
    expect(1).toEqual(1)
  })
})
