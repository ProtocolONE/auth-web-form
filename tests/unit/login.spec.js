// import factory from './utils/components-factory'
// import Login from '@/views/Login'

describe('Login', () => {
  // let wrapper = factory(Login)
  // let form = wrapper.find('form#login-form')

  // it('Draws the form', () => {
  //   expect(wrapper.find('form#login-form').exists()).toBeTruthy()
  // })
  //
  // it('Draws the form fields', () => {
  //   expect(form.find('input[type="email"]').exists()).toBeTruthy()
  //   expect(form.find('input[type="password"]').exists()).toBeTruthy()
  // })
  //
  // it('Draws an error when the email is empty', () => {
  //   wrapper.setData({ email: '' })
  //   form.trigger('submit.prevent')
  //   expect(form.find('input[type="email"] ~ .error').exists()).toBeTruthy()
  // })
  //
  // it('Draws an error when the password is empty', () => {
  //   wrapper.setData({ password: '' })
  //   form.trigger('submit.prevent')
  //   expect(form.find('input[type="password"] ~ .error').exists()).toBeTruthy()
  // })
  //
  // it('Check that the error has disappeared for the email', () => {
  //   wrapper.setData({ email: '' })
  //   form.trigger('submit.prevent')
  //   expect(form.find('input[type="email"] ~ .error').exists()).toBeTruthy()
  //   wrapper.setData({ email: 'test@test.com' })
  //   form.trigger('submit.prevent')
  //   expect(form.find('input[type="email"] ~ .error').exists()).toBeFalsy()
  // })
  //
  // it('Check that the error has disappeared for the password', () => {
  //   wrapper.setData({ password: '' })
  //   form.trigger('submit.prevent')
  //   expect(form.find('input[type="password"] ~ .error').exists()).toBeTruthy()
  //   wrapper.setData({ password: 'password' })
  //   form.trigger('submit.prevent')
  //   expect(form.find('input[type="password"] ~ .error').exists()).toBeFalsy()
  // })

  it('Test', () => {
    expect(1).toEqual(1)
  })
})
